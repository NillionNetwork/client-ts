{
  "name": "@nillion/client",
  "version": "0.1.0",
  "type": "module",
  "browser": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node esbuild.config.mjs",
    "build.watch": "node esbuild.config.mjs --watch",
    "test.build": "npx webpack --config webpack.config.mjs --watch",
    "test": "npx jasmine-browser-runner serve --config=jasmine.config.mjs"
  },
  "dependencies": {
    "@nillion/core": "^0.1.0",
    "@nillion/payments": "^0.1.0",
    "debug": "^4.3.5",
    "zod": "^3.23.8"
  }
}
